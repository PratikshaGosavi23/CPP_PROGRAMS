You are given an integer array nums.
Move all the zeroes to the end of the array, while keeping the order of non-zero elements same.
Do it in-place (don’t use another array).


#include <iostream>
#include <vector>
using namespace std;

void moveZeroes(vector<int>& nums) {
    int write = 0; // Position for next non-zero
    for (int read = 0; read < nums.size(); read++) {
        if (nums[read] != 0) {
            swap(nums[write], nums[read]);
            write++;
        }
    }
}

int main() {
    vector<int> nums;  // declare empty vector

    // insert values (since {} initialization not allowed in C++98)
    nums.push_back(0);
    nums.push_back(1);
    nums.push_back(0);
    nums.push_back(3);
    nums.push_back(12);

    // call the function
    moveZeroes(nums);

    // print result
    for (int i = 0; i < nums.size(); i++) {
        cout << nums[i] << " ";
    }
    return 0;
}

